#!/usr/bin/env bash
# ============================================================
#  App launcher — rofi drun (Catppuccin Mocha + flower banner)
# ============================================================
#  Theme lives in ~/.config/rofi/config.rasi (auto-loaded).
#  Bound to SUPER+R and SUPER+Space in hyprland.lua.
# ============================================================
exec rofi -show drun
